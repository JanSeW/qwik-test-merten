export default function Video({ src, className, cmsKey }) {
  return (
    <></>
    /*  <iframe
      width="500"
      height="315"
      data-attr-key={cmsKey}
      className={className}
      src={src}
    ></iframe> */
  );
}
